name: Web and core tests

on:
  pull_request:
  push:
    branches: [main, HK/deploy]

concurrency:
  group: web-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_OPTIONS: --max-old-space-size=4096

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type-check tests and imported core modules
        run: pnpm run typecheck:test

      - name: Run core tests with coverage
        run: pnpm run test:coverage

      - name: Build web and extension
        run: pnpm run build

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: core-coverage
          path: coverage/
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload extension package
        uses: actions/upload-artifact@v6
        with:
          name: ustudy-extension
          path: |
            dist-extension/
            public/downloads/ustudy-portal-sync.zip
          retention-days: 14
